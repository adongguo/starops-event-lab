# Repository Guidelines

- Keep every fixture synthetic and safe for a public repository.
- Never commit credentials, private logs, internal domains, or customer data.
- Run `python3 -m unittest discover -s tests` after code changes.
- Keep GitHub Actions permissions at the minimum required level.
- AI-generated fixes must remain proposals until a human reviews them.
